import 'package:flutter/material.dart';

class BotonGrandeComponent extends StatelessWidget {
  const BotonGrandeComponent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      width: 100,
      color: Colors.blue,
    );
  }
}

